<h1>My Tasks</h1>

<ul>
    <% @task.each do |task| %>
        <li><%= link_to task.title, task_path(task) %></li>
        <%= link_to 'Delete', task_path(task), method: :delete, data: { confirm: "Confirm?" } %>
    <% end %>
</ul>

<%= link_to 'Add new task', new_task_path %>